// import { create } from "zustand";

// const useStore = create((set) => ({
//   dataModal: {},
//   setDataModal: (presentData) => set((state) => ({ dataModal: presentData })),
//   //   setDataModal: (currentData) => set((prevData) => ({ dataModal: { ...prevData.dataModal, ...currentData } })),
// }));

// export default useStore;
